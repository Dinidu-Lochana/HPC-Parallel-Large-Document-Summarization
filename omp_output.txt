The document discusses a system for removing reflections from images, specifically those taken through glass. The system operates on linear (RAW) photos and can use an optional contextual photo to help disambiguate the reflection. It is trained on synthetic mixtures of real RAW photos and can produce high-quality, reflection-free images at full resolution. The system achieves state-of-the-art results on consumer photos taken in the field, outperforming prior works.

The removal of unwanted reflections in photos is a challenging task due to the diverse range of locations and situations in which they occur. Current methods to remove reflections have limitations, including restrictions on data collection and unrealistic training scenarios. The proposed system aims to address these challenges by improving the removal of reflections in various settings, such as shopping spots, buildings, and museums.

The system uses synthesized realistic and diverse photos, and a novel upsampler to produce results at the input image resolution. It also allows for optional contextual photos and outputs reflection and transmission images for user editing. This approach aims to improve the efficiency and effectiveness of removing reflections from photos.

The system consists of two models: a base model and an upsampler. The base model predicts texture and reflectance from a given image and an optional context image, while the upsampler iteratively refines these predictions using a Gaussian pyramid. The system is trained end-to-end from random weights using pre-trained perceptual losses and a custom modification of the StyleGAN modulation technique.

The proposed system is compared to prior works, such as Zhang et al. and DSRNet, and outperforms them when trained with RAW data. The importance of using RAW images is highlighted, as it allows for more accurate simulations and better handling of lighting effects. The system's performance is also evaluated through ablation studies, which highlight the importance of various components in achieving optimal results.

The system is effective in diverse settings, including museums, nature, and city scenes, and can recover correct and uniform colors. However, failures can occur when the reflection or the original image is too bright, causing the other to saturate to black, and errors can also occur when textured regions overlap.

The system's approach to upsampling and reflection editing is compared to V-DESIRR, and the authors' upsampler outperforms V-DESIRR, reducing error propagation by directly copying high-resolution features. The approach shows improved results over V-DESIRR, particularly in reducing artifacts.

The system's performance is evaluated through various metrics, including PSNR and SSIM, and the results demonstrate the effectiveness of the system in removing reflections and producing high-quality images.

Overall, the proposed system is a significant improvement over prior works in the field of reflection removal, and its ability to operate on RAW images and produce high-quality, reflection-free images at full resolution makes it a valuable tool for consumer photography. The system's flexibility, efficiency, and effectiveness make it a promising solution for a wide range of applications, from photography to computer vision.